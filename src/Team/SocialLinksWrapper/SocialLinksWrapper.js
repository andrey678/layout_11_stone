import styled from 'styled-components';

const StyledSocialLinksWrapper = styled.div`
  display: flex;
`;
const SocialLinksWrapper = (props) => <StyledSocialLinksWrapper {...props} />;

export default SocialLinksWrapper;
